---
phase: 06-parity-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types.rs
  - src/permissions.rs
  - src/host_resolution.rs
  - src/client.rs
  - src/adapter.rs
  - tests/integration.rs
autonomous: true
requirements:
  - PARITY-01
  - PARITY-05
must_haves:
  truths:
    - "All 18 TS methods that accept overrideHost now accept override_host: Option<&str> in Rust"
    - "queryAdvertisements filters out localhost and non-HTTPS hosts"
    - "MessageBoxClient tracks joined_rooms and exposes get_joined_rooms()"
    - "All 4 missing types (SendListParams, SendListResult, MessageBoxMultiQuote, Payment) exist in types.rs"
    - "cargo test compiles and all existing tests pass with updated call sites"
  artifacts:
    - path: "src/types.rs"
      provides: "SendListParams, SendListResult, SentRecipient, FailedRecipient, SendListTotals, MessageBoxMultiQuote, RecipientQuote, Payment types"
      contains: "SendListParams"
    - path: "src/permissions.rs"
      provides: "override_host param on all 9 permission/notification methods"
      exports: ["set_message_box_permission", "get_message_box_permission"]
    - path: "src/host_resolution.rs"
      provides: "localhost filter in query_advertisements, override_host on register_device/list_registered_devices"
      contains: "localhost"
    - path: "src/client.rs"
      provides: "joined_rooms HashSet field, get_joined_rooms, initialize_connection, test_socket, override_host on WS methods"
      contains: "joined_rooms"
  key_links:
    - from: "src/adapter.rs"
      to: "src/permissions.rs"
      via: "adapter calls permission methods with None override"
      pattern: "None.*override"
    - from: "src/adapter.rs"
      to: "src/client.rs"
      via: "adapter calls send_live_message/listen_for_live_messages with None override"
      pattern: "None.*override"
---

<objective>
Add all missing types, override_host parameter to 18 methods, queryAdvertisements localhost filter, joined_rooms tracking, and low-complexity missing methods (get_joined_rooms, initialize_connection, test_socket, init targetHost param).

Purpose: Close the mechanical API surface gaps so that the Rust crate has matching method signatures to the TS client. This is the prerequisite for PARITY-01 (API surface audit).

Output: All simple/mechanical gaps closed, cargo test green, ready for complex behavioral gaps in Plan 02.
</objective>

<execution_context>
@/Users/elisjackson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/elisjackson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-parity-verification/06-CONTEXT.md
@.planning/phases/06-parity-verification/06-RESEARCH.md
@.planning/research/TS_SOURCE_REFERENCE.md

<interfaces>
<!-- Current MessageBoxClient struct fields (from src/client.rs:18-36) -->
```rust
pub struct MessageBoxClient<W: WalletInterface + Clone + 'static> {
    host: String,
    auth_fetch: Mutex<AuthFetch<W>>,
    wallet: W,
    originator: Option<String>,
    identity_key: OnceCell<String>,
    pub(crate) init_once: OnceCell<()>,
    pub(crate) network: Network,
    ws_state: Mutex<Option<crate::websocket::MessageBoxWebSocket>>,
}
```

<!-- Current send_message signature (src/http_ops.rs:70-75) — Plan 02 changes this, not Plan 01 -->
```rust
pub async fn send_message(&self, recipient: &str, message_box: &str, body: &str) -> Result<String, MessageBoxError>
```

<!-- RemittanceAdapter delegates (src/adapter.rs) — must update call sites -->
```rust
// adapter calls: self.client.send_message(recipient, message_box, body)
// adapter calls: self.client.acknowledge_message(message_ids)
// adapter calls: self.client.send_live_message(recipient, message_box, body)
// adapter calls: self.client.listen_for_live_messages(message_box, on_message)
// adapter calls: self.client.list_messages(message_box)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add missing types and override_host to simple methods (permissions, notifications, devices, WS accessors)</name>
  <files>src/types.rs, src/permissions.rs, src/host_resolution.rs, src/client.rs, src/adapter.rs, tests/integration.rs</files>
  <action>
**Step 1 — Add missing types to src/types.rs:**

Add these types with `#[derive(Serialize, Deserialize, Clone, Debug)]` and `#[serde(rename_all = "camelCase")]`:

- `SendListParams { recipients: Vec<String>, message_box: String, body: String, skip_encryption: Option<bool> }`
- `SendListResult { status: String, description: String, sent: Vec<SentRecipient>, blocked: Vec<String>, failed: Vec<FailedRecipient>, totals: Option<SendListTotals> }`
- `SentRecipient { recipient: String, message_id: String }`
- `FailedRecipient { recipient: String, error: String }`
- `SendListTotals { delivery_fees: i64, recipient_fees: i64, total_for_payable_recipients: i64 }`
- `MessageBoxMultiQuote { quotes_by_recipient: Vec<RecipientQuote>, totals: Option<SendListTotals>, blocked_recipients: Vec<String>, delivery_agent_identity_key_by_host: std::collections::HashMap<String, String> }`
- `RecipientQuote { recipient: String, message_box: String, delivery_fee: i64, recipient_fee: i64, status: String }`
- `Payment { tx: Vec<u8>, outputs: Vec<PaymentOutput>, description: String, labels: Option<Vec<String>>, seek_permission: Option<bool> }` where `PaymentOutput { output_index: u32, protocol: String, payment_remittance: Option<PaymentRemittanceInfo>, insertion_remittance: Option<InsertionRemittanceInfo> }` — use `#[serde(skip_serializing_if = "Option::is_none")]` on optional fields

Also add `PaymentRemittanceInfo { derivation_prefix: String, derivation_suffix: String, sender_identity_key: String }` and `InsertionRemittanceInfo { basket: String, custom_instructions: Option<String>, tags: Option<Vec<String>> }`.

Export all new types from `lib.rs` if needed.

**Step 2 — Add override_host to 9 simple permission/notification/device methods:**

In `src/permissions.rs`, add `override_host: Option<&str>` as last parameter to ALL 9 methods:
- `set_message_box_permission` — use `override_host.unwrap_or(self.host())` for URL base
- `get_message_box_permission` — same pattern
- `list_message_box_permissions` — same pattern
- `get_message_box_quote` — same pattern (single-recipient path only; multi-recipient added in Plan 02)
- `allow_notifications_from_peer` — same pattern
- `deny_notifications_from_peer` — same pattern
- `check_peer_notification_status` — same pattern
- `list_peer_notifications` — same pattern
- `send_notification` — same pattern; also pass override_host through to internal `send_message` call once Plan 02 adds it (for now pass through to any internal calls that accept it)

In `src/host_resolution.rs`, add `override_host: Option<&str>` to:
- `register_device` — use `override_host.unwrap_or(self.host())`
- `list_registered_devices` — same pattern

**Step 3 — Add localhost filter to query_advertisements:**

In `src/host_resolution.rs`, after decoding the host string from PushDrop fields, add:
```rust
// TS parity: filter localhost and non-HTTPS hosts
if !host_str.starts_with("https://") || host_str.contains("localhost") {
    continue;
}
```

This goes inside the loop that processes overlay results, right after the `String::from_utf8` call for the host field.

**Step 4 — Add joined_rooms tracking + accessors to MessageBoxClient:**

In `src/client.rs`:
1. Add field to struct: `joined_rooms: Mutex<std::collections::HashSet<String>>`
2. Initialize in `new()`: `joined_rooms: Mutex::new(std::collections::HashSet::new())`
3. Add `pub fn get_joined_rooms(&self) -> ... ` that locks the mutex and returns a clone of the HashSet
4. Add `#[cfg(test)] pub fn test_socket(&self) -> ... ` that returns `Option<bool>` indicating whether ws_state is Some (returning the actual socket type is impractical — just return whether connected, matching the spirit of TS accessor)
5. Add `pub async fn initialize_connection(&self, override_host: Option<&str>) -> Result<(), MessageBoxError>` that calls the existing private `ensure_ws_connected` (pass override_host through or use it as the WS host)
6. Update `init(&self)` signature to `init(&self, target_host: Option<&str>)` — when Some, use that host for anoint_host instead of self.host()
7. Add `override_host: Option<&str>` to `send_live_message`, `listen_for_live_messages`, `leave_room` (note: `join_room` is currently on MessageBoxWebSocket, not MessageBoxClient — add a public `join_room` on MessageBoxClient that delegates to the internal WS `join_room`)
8. In the public `join_room` and `leave_room`, update `self.joined_rooms` HashSet (insert on join, remove on leave)

**Step 5 — Update adapter.rs call sites:**

Update ALL method calls in `src/adapter.rs` to pass `None` as the new `override_host` parameter. The adapter already has its own `host_override` handling — it should pass `self.host_override.as_deref()` instead of `None` where appropriate (for permission methods where it currently uses `self.host_override`).

Also update `assert_initialized` call in adapter — `init()` now takes `Option<&str>`, pass `None`.

**Step 6 — Update test call sites:**

In `tests/integration.rs` and all `#[cfg(test)] mod tests` blocks in source files, update every call to the modified methods to include `None` as the override_host parameter.

For `init()` calls, add `None` parameter.

Grep for all affected method names and update systematically. Expected: ~30-40 call sites across test code.
  </action>
  <verify>
    <automated>cd /Users/elisjackson/Projects/rust-messagebox-client && cargo test --lib 2>&1 | tail -10</automated>
  </verify>
  <done>
- All 4 new type families (SendListParams, SendListResult, MessageBoxMultiQuote, Payment) exist in types.rs with serde derives
- All 9 permission/notification methods have override_host: Option&lt;&str&gt; parameter
- register_device and list_registered_devices have override_host parameter
- query_advertisements filters localhost and non-HTTPS hosts
- MessageBoxClient has joined_rooms field, get_joined_rooms(), test_socket(), initialize_connection(), public join_room()
- init() accepts optional target_host parameter
- send_live_message, listen_for_live_messages have override_host
- All adapter.rs call sites updated
- All test call sites updated
- cargo test passes with 0 failures
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify full test suite and confirm no regressions</name>
  <files>src/lib.rs</files>
  <action>
Run the full test suite including integration tests. Ensure all new types are exported from lib.rs (add `pub use types::{SendListParams, SendListResult, ...}` if not already re-exported). Fix any compilation errors from missed call sites.

Verify the localhost filter works by checking that `query_advertisements` code path has the filter in place.

Ensure `cargo build` produces no warnings related to the new code (allow existing warnings from earlier phases).
  </action>
  <verify>
    <automated>cd /Users/elisjackson/Projects/rust-messagebox-client && cargo test 2>&1 | tail -10</automated>
  </verify>
  <done>
- `cargo test` full suite passes (lib + integration tests)
- All new types exported from lib.rs
- No new warnings from Phase 6 code
- Ready for Plan 02 complex behavioral changes
  </done>
</task>

</tasks>

<verification>
- `cargo test --lib` passes (unit tests)
- `cargo test` passes (unit + integration tests)
- `cargo build` clean
- grep confirms override_host present on all 18 target methods
- grep confirms localhost filter in query_advertisements
- grep confirms joined_rooms field on MessageBoxClient
</verification>

<success_criteria>
1. All 4 missing type families exist in types.rs with correct serde annotations
2. 13 methods have override_host parameter added (9 permission/notification + 2 device + 2 WS)
3. query_advertisements filters localhost and non-HTTPS hosts
4. MessageBoxClient tracks joined_rooms and exposes get_joined_rooms, test_socket, initialize_connection, public join_room
5. init() accepts optional target_host
6. All existing tests pass with updated call sites
7. No compilation errors or new warnings
</success_criteria>

<output>
After completion, create `.planning/phases/06-parity-verification/06-01-SUMMARY.md`
</output>
